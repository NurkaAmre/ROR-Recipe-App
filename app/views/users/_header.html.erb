<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid d-flex">
  <%= link_to 'Recipe App', root_path, class: 'navbar-brand'%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'Foods', foods_path, class: 'nav-link'%>
        </li>
         <li class="nav-item">
        <%= link_to 'Recipes', recipes_path, class: 'nav-link'%>
        </li>
        <li class='nav-link'>Public Recipes</li>
        <li class='nav-link'>Shopping List</li>
        <li class='nav-link text-white'><%=current_user.name%></li>
        <li class="d-flex ml-auto mb-3 mt-3">
          <% if user_signed_in? %>
            <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'btn btn-warning  text-end sign', data: { turbo: false } %>
          <% else %>
            <%= button_to 'Sign In', new_user_session_path, class: 'btn btn-warning  text-end sign'%>
          <% end%>
        </li>
      </ul>
        
    </div>
  </div>
</nav>